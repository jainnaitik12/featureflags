# Supabase Session pooler (IPv4) — from Dashboard → Settings → Database → Connection string
DATABASE_URL=postgresql://postgres.gbfvezbdkiumqocnpgcf:NaitikJain1@aws-1-ap-south-1.pooler.supabase.com:5432/postgres?sslmode=require

# To use the local Postgres container instead, comment the line above and use:
# DATABASE_URL=postgresql://postgres:featureflags@postgres:5432/postgres?sslmode=disable
